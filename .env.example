# LLM Configuration
LLM_PROVIDER=gemini          # gemini or openai
LLM_MODEL=                   # optional; overrides provider default model
GEMINI_API_KEY=              # required when LLM_PROVIDER=gemini
OPENAI_API_KEY=              # required when LLM_PROVIDER=openai
